import { buildWorld } from '../worldFactory';

export const world05 = buildWorld(
  {
    id: 5,
    name: 'Castle Marketplace',
    subtitle: 'E-commerce joins',
    theme: 'castle',
    description: 'Products, sellers, orders — revenue from line items and shipped orders.',
    database: 'castle_shop.sql',
    icon: '🏰',
    color: '#c084fc',
    prerequisites: [4],
  },
  [
    {
      title: 'Category roll call',
      isBoss: false,
      story: 'List every category name.',
      concept: 'SELECT',
      task: 'name FROM categories.',
      starterCode: 'SELECT name FROM categories;',
      expectedQuery: 'SELECT name FROM categories;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 45,
      xpReward: 50,
      relevantTables: ['categories'],
    },
    {
      title: 'Gadget aisle',
      isBoss: false,
      story: 'Electronics is category_id 1.',
      concept: 'WHERE',
      task: 'name, price FROM products WHERE category_id = 1.',
      starterCode: 'SELECT name, price FROM products WHERE category_id = 1;',
      expectedQuery: 'SELECT name, price FROM products WHERE category_id = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 90,
      xpReward: 70,
      relevantTables: ['products'],
    },
    {
      title: 'Top-rated shops',
      isBoss: false,
      story: 'Sellers rated above 4.5.',
      concept: 'WHERE REAL',
      task: 'shop_name FROM sellers WHERE rating > 4.5.',
      starterCode: 'SELECT shop_name FROM sellers WHERE rating > 4.5;',
      expectedQuery: 'SELECT shop_name FROM sellers WHERE rating > 4.5;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 90,
      xpReward: 70,
      relevantTables: ['sellers'],
    },
    {
      title: 'Who sells what',
      isBoss: false,
      story: 'Product name with its seller shop name.',
      concept: 'JOIN',
      task: 'products JOIN sellers ON seller_id; return products.name, sellers.shop_name.',
      starterCode:
        'SELECT p.name, s.shop_name\nFROM products p\nJOIN sellers s ON p.seller_id = s.id;',
      expectedQuery: `SELECT p.name, s.shop_name
FROM products p
JOIN sellers s ON p.seller_id = s.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 8 },
      parTimeSeconds: 200,
      xpReward: 110,
      relevantTables: ['products', 'sellers'],
    },
    {
      title: 'Big Apple buyers',
      isBoss: false,
      story: 'Customers in NYC.',
      concept: 'WHERE TEXT',
      task: 'email FROM customers WHERE city = NYC.',
      starterCode: "SELECT email FROM customers WHERE city = 'NYC';",
      expectedQuery: "SELECT email FROM customers WHERE city = 'NYC';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 90,
      xpReward: 65,
      relevantTables: ['customers'],
    },
    {
      title: 'Shipped tally',
      isBoss: false,
      story: 'How many orders are shipped?',
      concept: 'COUNT + WHERE',
      task: 'COUNT(*) AS n FROM orders WHERE status = shipped.',
      starterCode: "SELECT COUNT(*) AS n FROM orders WHERE status = 'shipped';",
      expectedQuery: "SELECT COUNT(*) AS n FROM orders WHERE status = 'shipped';",
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['n'],
      },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['orders'],
    },
    {
      title: 'Mega-cart lines',
      isBoss: false,
      story: 'How many line items on order_id 5?',
      concept: 'COUNT filter',
      task: 'COUNT(*) FROM order_lines WHERE order_id = 5.',
      starterCode: 'SELECT COUNT(*) AS lines FROM order_lines WHERE order_id = 5;',
      expectedQuery: 'SELECT COUNT(*) AS lines FROM order_lines WHERE order_id = 5;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['lines'],
      },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['order_lines'],
    },
    {
      title: 'Low stock alert',
      isBoss: false,
      story: 'Products with fewer than 20 units in stock.',
      concept: 'WHERE INTEGER',
      task: 'name FROM products WHERE stock < 20.',
      starterCode: 'SELECT name FROM products WHERE stock < 20;',
      expectedQuery: 'SELECT name FROM products WHERE stock < 20;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 90,
      xpReward: 80,
      relevantTables: ['products'],
    },
    {
      title: 'Catalog breadth',
      isBoss: false,
      story: 'How many products per category?',
      concept: 'GROUP BY',
      task: 'category_id, COUNT(*) AS n FROM products GROUP BY category_id.',
      starterCode:
        'SELECT category_id, COUNT(*) AS n FROM products GROUP BY category_id;',
      expectedQuery: 'SELECT category_id, COUNT(*) AS n FROM products GROUP BY category_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 120,
      xpReward: 95,
      relevantTables: ['products'],
    },
    {
      title: 'Castle Boss: shipped revenue king',
      isBoss: true,
      story:
        'Which seller_id earned the most from shipped orders? Use line revenue qty * unit_price; tie-break lowest seller_id.',
      concept: 'JOIN + aggregate',
      task: 'JOIN order_lines → products → orders; filter shipped; SUM(qty*unit_price) GROUP BY seller_id; ORDER BY rev DESC, seller_id ASC LIMIT 1.',
      starterCode:
        'SELECT p.seller_id, SUM(ol.qty * ol.unit_price) AS rev\nFROM order_lines ol\nJOIN products p ON ol.product_id = p.id\nJOIN orders o ON ol.order_id = o.id\nWHERE o.status = \'shipped\'\nGROUP BY p.seller_id\nORDER BY ',
      expectedQuery: `SELECT p.seller_id, SUM(ol.qty * ol.unit_price) AS rev
FROM order_lines ol
JOIN products p ON ol.product_id = p.id
JOIN orders o ON ol.order_id = o.id
WHERE o.status = 'shipped'
GROUP BY p.seller_id
ORDER BY rev DESC, p.seller_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['seller_id', 'rev'],
      },
      parTimeSeconds: 320,
      xpReward: 300,
      relevantTables: ['order_lines', 'products', 'orders'],
    },
  ],
);

export const world06 = buildWorld(
  {
    id: 6,
    name: 'Storm Street',
    subtitle: 'Markets & volume',
    theme: 'finance',
    description: 'Tickers, daily prices, trades — slices, sums, and ranking.',
    database: 'storm_stocks.sql',
    icon: '📈',
    color: '#fbbf24',
    prerequisites: [5],
  },
  [
    {
      title: 'Ticker tape',
      isBoss: false,
      story: 'Every ticker and company name.',
      concept: 'SELECT',
      task: 'ticker, company FROM stocks.',
      starterCode: 'SELECT ticker, company FROM stocks;',
      expectedQuery: 'SELECT ticker, company FROM stocks;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 6 },
      parTimeSeconds: 60,
      xpReward: 55,
      relevantTables: ['stocks'],
    },
    {
      title: 'Tech watchlist',
      isBoss: false,
      story: 'Stocks in the Tech sector (sector name Tech).',
      concept: 'JOIN',
      task: 'stocks JOIN sectors; filter name = Tech; return ticker.',
      starterCode:
        'SELECT s.ticker\nFROM stocks s\nJOIN sectors sec ON s.sector_id = sec.id\nWHERE sec.name = \'Tech\';',
      expectedQuery: `SELECT s.ticker
FROM stocks s
JOIN sectors sec ON s.sector_id = sec.id
WHERE sec.name = 'Tech';`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 180,
      xpReward: 100,
      relevantTables: ['stocks', 'sectors'],
    },
    {
      title: 'QFAI close',
      isBoss: false,
      story: 'Closing price for stock_id 1 on 2025-03-03.',
      concept: 'Compound WHERE',
      task: 'close_price WHERE stock_id=1 AND trade_date = 2025-03-03.',
      starterCode:
        "SELECT close_price FROM daily_prices WHERE stock_id = 1 AND trade_date = '2025-03-03';",
      expectedQuery:
        "SELECT close_price FROM daily_prices WHERE stock_id = 1 AND trade_date = '2025-03-03';",
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['close_price'],
      },
      parTimeSeconds: 120,
      xpReward: 80,
      relevantTables: ['daily_prices'],
    },
    {
      title: 'Peak close',
      isBoss: false,
      story: 'Highest closing price in the whole daily_prices table.',
      concept: 'MAX',
      task: 'MAX(close_price) AS hi FROM daily_prices.',
      starterCode: 'SELECT MAX(close_price) AS hi FROM daily_prices;',
      expectedQuery: 'SELECT MAX(close_price) AS hi FROM daily_prices;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['hi'],
      },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['daily_prices'],
    },
    {
      title: 'Buy side',
      isBoss: false,
      story: 'All buy trades.',
      concept: 'WHERE ENUM',
      task: 'id, trader_id, shares FROM trades WHERE side = buy.',
      starterCode: "SELECT id, trader_id, shares FROM trades WHERE side = 'buy';",
      expectedQuery: "SELECT id, trader_id, shares FROM trades WHERE side = 'buy';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['trades'],
    },
    {
      title: 'Trader activity',
      isBoss: false,
      story: 'Trade count per trader.',
      concept: 'GROUP BY',
      task: 'trader_id, COUNT(*) AS n FROM trades GROUP BY trader_id.',
      starterCode: 'SELECT trader_id, COUNT(*) AS n FROM trades GROUP BY trader_id;',
      expectedQuery: 'SELECT trader_id, COUNT(*) AS n FROM trades GROUP BY trader_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 120,
      xpReward: 95,
      relevantTables: ['trades'],
    },
    {
      title: 'AI tickers',
      isBoss: false,
      story: 'Tickers containing AI.',
      concept: 'LIKE',
      task: 'ticker FROM stocks WHERE ticker LIKE %AI%.',
      starterCode: "SELECT ticker FROM stocks WHERE ticker LIKE '%AI%';",
      expectedQuery: "SELECT ticker FROM stocks WHERE ticker LIKE '%AI%';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 1 },
      parTimeSeconds: 90,
      xpReward: 70,
      relevantTables: ['stocks'],
    },
    {
      title: 'QFAI history length',
      isBoss: false,
      story: 'How many daily price rows for stock_id 1?',
      concept: 'COUNT',
      task: 'COUNT(*) FROM daily_prices WHERE stock_id = 1.',
      starterCode: 'SELECT COUNT(*) AS n FROM daily_prices WHERE stock_id = 1;',
      expectedQuery: 'SELECT COUNT(*) AS n FROM daily_prices WHERE stock_id = 1;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['n'],
      },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['daily_prices'],
    },
    {
      title: 'Day P&L hint',
      isBoss: false,
      story: 'Per-row gain: close minus open for stock_id 1.',
      concept: 'Expressions',
      task: 'trade_date, (close_price - open_price) AS move for stock_id = 1.',
      starterCode:
        'SELECT trade_date, (close_price - open_price) AS move\nFROM daily_prices\nWHERE stock_id = 1;',
      expectedQuery: `SELECT trade_date, (close_price - open_price) AS move
FROM daily_prices
WHERE stock_id = 1;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 150,
      xpReward: 100,
      relevantTables: ['daily_prices'],
    },
    {
      title: 'Storm Boss: liquidity leader',
      isBoss: true,
      story: 'Which stock_id has the highest total daily volume? Tie-break smallest stock_id.',
      concept: 'SUM + GROUP BY',
      task: 'SUM(volume) GROUP BY stock_id ORDER BY total_vol DESC, stock_id ASC LIMIT 1.',
      starterCode:
        'SELECT stock_id, SUM(volume) AS total_vol\nFROM daily_prices\nGROUP BY stock_id\nORDER BY ',
      expectedQuery: `SELECT stock_id, SUM(volume) AS total_vol
FROM daily_prices
GROUP BY stock_id
ORDER BY total_vol DESC, stock_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['stock_id', 'total_vol'],
      },
      parTimeSeconds: 240,
      xpReward: 295,
      relevantTables: ['daily_prices'],
    },
  ],
);

export const world07 = buildWorld(
  {
    id: 7,
    name: 'Arena Analytics',
    subtitle: 'SaaS product data',
    theme: 'saas',
    description: 'Teams, users, events, subscriptions — product analytics patterns.',
    database: 'arena_saas.sql',
    icon: '🎯',
    color: '#2dd4bf',
    prerequisites: [6],
  },
  [
    {
      title: 'Enterprise accounts',
      isBoss: false,
      story: 'Team names on the enterprise plan.',
      concept: 'WHERE',
      task: 'name FROM teams WHERE plan = enterprise.',
      starterCode: "SELECT name FROM teams WHERE plan = 'enterprise';",
      expectedQuery: "SELECT name FROM teams WHERE plan = 'enterprise';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 1 },
      parTimeSeconds: 75,
      xpReward: 60,
      relevantTables: ['teams'],
    },
    {
      title: 'Seats per team',
      isBoss: false,
      story: 'How many users in each team?',
      concept: 'GROUP BY',
      task: 'team_id, COUNT(*) AS n FROM users GROUP BY team_id.',
      starterCode: 'SELECT team_id, COUNT(*) AS n FROM users GROUP BY team_id;',
      expectedQuery: 'SELECT team_id, COUNT(*) AS n FROM users GROUP BY team_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 120,
      xpReward: 90,
      relevantTables: ['users'],
    },
    {
      title: 'Login stream',
      isBoss: false,
      story: 'Events named login.',
      concept: 'WHERE',
      task: 'user_id, event_time FROM events WHERE event_name = login.',
      starterCode: "SELECT user_id, event_time FROM events WHERE event_name = 'login';",
      expectedQuery: "SELECT user_id, event_time FROM events WHERE event_name = 'login';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['events'],
    },
    {
      title: 'Paying teams',
      isBoss: false,
      story: 'Teams with active subscription and mrr > 0.',
      concept: 'WHERE multi',
      task: 'team_id, mrr FROM subscriptions WHERE active = 1 AND mrr > 0.',
      starterCode: 'SELECT team_id, mrr FROM subscriptions WHERE active = 1 AND mrr > 0;',
      expectedQuery: 'SELECT team_id, mrr FROM subscriptions WHERE active = 1 AND mrr > 0;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 120,
      xpReward: 85,
      relevantTables: ['subscriptions'],
    },
    {
      title: 'Gamma roster',
      isBoss: false,
      story: 'Emails for users on team_id 3.',
      concept: 'WHERE FK',
      task: 'email FROM users WHERE team_id = 3.',
      starterCode: 'SELECT email FROM users WHERE team_id = 3;',
      expectedQuery: 'SELECT email FROM users WHERE team_id = 3;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['users'],
    },
    {
      title: 'User timeline',
      isBoss: false,
      story: 'All events for user_id 1.',
      concept: 'ORDER BY',
      task: 'event_name, event_time WHERE user_id = 1 ORDER BY event_time.',
      starterCode:
        "SELECT event_name, event_time FROM events WHERE user_id = 1 ORDER BY event_time;",
      expectedQuery:
        "SELECT event_name, event_time FROM events WHERE user_id = 1 ORDER BY event_time;",
      validation: { strategy: ['result_match'], orderSensitive: true, expectedRowCount: 3 },
      parTimeSeconds: 120,
      xpReward: 85,
      relevantTables: ['events'],
    },
    {
      title: 'Top MRR',
      isBoss: false,
      story: 'Largest monthly recurring revenue value.',
      concept: 'MAX',
      task: 'MAX(mrr) AS top_mrr FROM subscriptions.',
      starterCode: 'SELECT MAX(mrr) AS top_mrr FROM subscriptions;',
      expectedQuery: 'SELECT MAX(mrr) AS top_mrr FROM subscriptions;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['top_mrr'],
      },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['subscriptions'],
    },
    {
      title: 'March activity',
      isBoss: false,
      story: 'Events on or after 2025-03-01 (prefix filter).',
      concept: 'LIKE / comparison',
      task: 'COUNT(*) WHERE event_time >= 2025-03-01 or LIKE 2025-03%.',
      starterCode: "SELECT COUNT(*) AS n FROM events WHERE event_time LIKE '2025-03%';",
      expectedQuery: "SELECT COUNT(*) AS n FROM events WHERE event_time LIKE '2025-03%';",
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['n'],
      },
      parTimeSeconds: 100,
      xpReward: 80,
      relevantTables: ['events'],
    },
    {
      title: 'Query runners',
      isBoss: false,
      story: 'Users who triggered event_name query_run.',
      concept: 'DISTINCT',
      task: 'DISTINCT user_id FROM events WHERE event_name = query_run.',
      starterCode: "SELECT DISTINCT user_id FROM events WHERE event_name = 'query_run';",
      expectedQuery: "SELECT DISTINCT user_id FROM events WHERE event_name = 'query_run';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 110,
      xpReward: 85,
      relevantTables: ['events'],
    },
    {
      title: 'Arena Boss: power user',
      isBoss: true,
      story: 'user_id with the most events; tie-break smallest user_id.',
      concept: 'GROUP BY + ORDER BY',
      task: 'COUNT(*) GROUP BY user_id ORDER BY c DESC, user_id ASC LIMIT 1.',
      starterCode:
        'SELECT user_id, COUNT(*) AS c\nFROM events\nGROUP BY user_id\nORDER BY c DESC, user_id ASC\nLIMIT 1;',
      expectedQuery: `SELECT user_id, COUNT(*) AS c
FROM events
GROUP BY user_id
ORDER BY c DESC, user_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['user_id', 'c'],
      },
      parTimeSeconds: 220,
      xpReward: 290,
      relevantTables: ['events'],
    },
  ],
);

export const world08 = buildWorld(
  {
    id: 8,
    name: 'Workshop Floor',
    subtitle: 'Manufacturing',
    theme: 'factory',
    description: 'Suppliers, parts, work orders, inventory moves.',
    database: 'workshop_parts.sql',
    icon: '⚙️',
    color: '#94a3b8',
    prerequisites: [7],
  },
  [
    {
      title: 'SKU list',
      isBoss: false,
      story: 'Every part sku and unit_cost.',
      concept: 'SELECT',
      task: 'sku, unit_cost FROM parts.',
      starterCode: 'SELECT sku, unit_cost FROM parts;',
      expectedQuery: 'SELECT sku, unit_cost FROM parts;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 6 },
      parTimeSeconds: 60,
      xpReward: 55,
      relevantTables: ['parts'],
    },
    {
      title: 'FastBolt catalog',
      isBoss: false,
      story: 'Parts from supplier_id 1 (FastBolt).',
      concept: 'WHERE',
      task: 'sku FROM parts WHERE supplier_id = 1.',
      starterCode: 'SELECT sku FROM parts WHERE supplier_id = 1;',
      expectedQuery: 'SELECT sku FROM parts WHERE supplier_id = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['parts'],
    },
    {
      title: 'Open work',
      isBoss: false,
      story: 'Work orders still open.',
      concept: 'WHERE status',
      task: 'id, part_id, qty FROM work_orders WHERE status = open.',
      starterCode: "SELECT id, part_id, qty FROM work_orders WHERE status = 'open';",
      expectedQuery: "SELECT id, part_id, qty FROM work_orders WHERE status = 'open';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['work_orders'],
    },
    {
      title: 'Open load',
      isBoss: false,
      story: 'Total units ordered across open work orders.',
      concept: 'SUM + WHERE',
      task: 'SUM(qty) FROM work_orders WHERE status = open.',
      starterCode: "SELECT SUM(qty) AS units FROM work_orders WHERE status = 'open';",
      expectedQuery: "SELECT SUM(qty) AS units FROM work_orders WHERE status = 'open';",
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['units'],
      },
      parTimeSeconds: 120,
      xpReward: 85,
      relevantTables: ['work_orders'],
    },
    {
      title: 'Work order detail',
      isBoss: false,
      story: 'Each work order with part description.',
      concept: 'JOIN',
      task: 'work_orders JOIN parts ON part_id; return work_orders.id, parts.description.',
      starterCode:
        'SELECT w.id, p.description\nFROM work_orders w\nJOIN parts p ON w.part_id = p.id;',
      expectedQuery: `SELECT w.id, p.description
FROM work_orders w
JOIN parts p ON w.part_id = p.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 6 },
      parTimeSeconds: 180,
      xpReward: 105,
      relevantTables: ['work_orders', 'parts'],
    },
    {
      title: 'Consumption moves',
      isBoss: false,
      story: 'Inventory moves with negative delta_qty (consumption).',
      concept: 'WHERE signed',
      task: 'part_id, delta_qty WHERE delta_qty < 0.',
      starterCode: 'SELECT part_id, delta_qty FROM inventory_moves WHERE delta_qty < 0;',
      expectedQuery: 'SELECT part_id, delta_qty FROM inventory_moves WHERE delta_qty < 0;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 90,
      xpReward: 80,
      relevantTables: ['inventory_moves'],
    },
    {
      title: 'Parts per supplier',
      isBoss: false,
      story: 'How many parts does each supplier provide?',
      concept: 'GROUP BY',
      task: 'supplier_id, COUNT(*) AS n FROM parts GROUP BY supplier_id.',
      starterCode: 'SELECT supplier_id, COUNT(*) AS n FROM parts GROUP BY supplier_id;',
      expectedQuery: 'SELECT supplier_id, COUNT(*) AS n FROM parts GROUP BY supplier_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 110,
      xpReward: 90,
      relevantTables: ['parts'],
    },
    {
      title: 'Slowest supplier',
      isBoss: false,
      story: 'Supplier name with the longest lead_time_days.',
      concept: 'ORDER BY',
      task: 'name FROM suppliers ORDER BY lead_time_days DESC LIMIT 1.',
      starterCode: 'SELECT name FROM suppliers ORDER BY lead_time_days DESC LIMIT 1;',
      expectedQuery: 'SELECT name FROM suppliers ORDER BY lead_time_days DESC LIMIT 1;',
      validation: { strategy: ['result_match'], orderSensitive: true, expectedRowCount: 1 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['suppliers'],
    },
    {
      title: 'Steel spend base',
      isBoss: false,
      story: 'List sku and unit_cost where description contains Steel.',
      concept: 'LIKE',
      task: 'sku, unit_cost FROM parts WHERE description LIKE %Steel%.',
      starterCode: "SELECT sku, unit_cost FROM parts WHERE description LIKE '%Steel%';",
      expectedQuery: "SELECT sku, unit_cost FROM parts WHERE description LIKE '%Steel%';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 100,
      xpReward: 80,
      relevantTables: ['parts'],
    },
    {
      title: 'Workshop Boss: hottest part',
      isBoss: true,
      story: 'part_id with the largest SUM(qty) in work_orders; tie-break smallest part_id.',
      concept: 'SUM + GROUP BY',
      task: 'SUM(qty) GROUP BY part_id ORDER BY q DESC, part_id ASC LIMIT 1.',
      starterCode:
        'SELECT part_id, SUM(qty) AS q\nFROM work_orders\nGROUP BY part_id\nORDER BY q DESC, part_id ASC\nLIMIT 1;',
      expectedQuery: `SELECT part_id, SUM(qty) AS q
FROM work_orders
GROUP BY part_id
ORDER BY q DESC, part_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['part_id', 'q'],
      },
      parTimeSeconds: 230,
      xpReward: 295,
      relevantTables: ['work_orders'],
    },
  ],
);

export const world09 = buildWorld(
  {
    id: 9,
    name: 'Stream State',
    subtitle: 'Music catalog',
    theme: 'music',
    description: 'Artists, albums, tracks, plays — fan engagement.',
    database: 'music_stream.sql',
    icon: '🎵',
    color: '#f472b6',
    prerequisites: [8],
  },
  [
    {
      title: 'Pop stars',
      isBoss: false,
      story: 'Artists in genre Pop.',
      concept: 'WHERE',
      task: 'name FROM artists WHERE genre = Pop.',
      starterCode: "SELECT name FROM artists WHERE genre = 'Pop';",
      expectedQuery: "SELECT name FROM artists WHERE genre = 'Pop';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 1 },
      parTimeSeconds: 60,
      xpReward: 55,
      relevantTables: ['artists'],
    },
    {
      title: 'Nova discography',
      isBoss: false,
      story: 'Album titles for artist_id 1.',
      concept: 'WHERE FK',
      task: 'title FROM albums WHERE artist_id = 1.',
      starterCode: 'SELECT title FROM albums WHERE artist_id = 1;',
      expectedQuery: 'SELECT title FROM albums WHERE artist_id = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['albums'],
    },
    {
      title: 'Long cuts',
      isBoss: false,
      story: 'Tracks longer than 200 seconds.',
      concept: 'WHERE INTEGER',
      task: 'title FROM tracks WHERE duration_sec > 200.',
      starterCode: 'SELECT title FROM tracks WHERE duration_sec > 200;',
      expectedQuery: 'SELECT title FROM tracks WHERE duration_sec > 200;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['tracks'],
    },
    {
      title: 'Track in context',
      isBoss: false,
      story: 'Track title with album title.',
      concept: 'JOIN',
      task: 'tracks JOIN albums; return tracks.title, albums.title AS album_title.',
      starterCode:
        'SELECT t.title, a.title AS album_title\nFROM tracks t\nJOIN albums a ON t.album_id = a.id;',
      expectedQuery: `SELECT t.title, a.title AS album_title
FROM tracks t
JOIN albums a ON t.album_id = a.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 7 },
      parTimeSeconds: 200,
      xpReward: 115,
      relevantTables: ['tracks', 'albums'],
    },
    {
      title: 'Tracks per album',
      isBoss: false,
      story: 'COUNT tracks grouped by album_id.',
      concept: 'GROUP BY',
      task: 'album_id, COUNT(*) AS n FROM tracks GROUP BY album_id.',
      starterCode: 'SELECT album_id, COUNT(*) AS n FROM tracks GROUP BY album_id;',
      expectedQuery: 'SELECT album_id, COUNT(*) AS n FROM tracks GROUP BY album_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 5 },
      parTimeSeconds: 120,
      xpReward: 95,
      relevantTables: ['tracks'],
    },
    {
      title: 'US listeners',
      isBoss: false,
      story: 'Listener handles from the US.',
      concept: 'WHERE',
      task: 'handle FROM listeners WHERE country = US.',
      starterCode: "SELECT handle FROM listeners WHERE country = 'US';",
      expectedQuery: "SELECT handle FROM listeners WHERE country = 'US';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 1 },
      parTimeSeconds: 60,
      xpReward: 55,
      relevantTables: ['listeners'],
    },
    {
      title: 'sky_fan plays',
      isBoss: false,
      story: 'How many plays for listener_id 1?',
      concept: 'COUNT',
      task: 'COUNT(*) FROM plays WHERE listener_id = 1.',
      starterCode: 'SELECT COUNT(*) AS n FROM plays WHERE listener_id = 1;',
      expectedQuery: 'SELECT COUNT(*) AS n FROM plays WHERE listener_id = 1;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['n'],
      },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['plays'],
    },
    {
      title: 'Album stream totals',
      isBoss: false,
      story: 'SUM streams_millions per album via tracks.',
      concept: 'JOIN + SUM',
      task: 'albums.id, SUM(t.streams_millions) — JOIN tracks to albums, GROUP BY albums.id.',
      starterCode:
        'SELECT a.id, SUM(t.streams_millions) AS streams\nFROM tracks t\nJOIN albums a ON t.album_id = a.id\nGROUP BY a.id;',
      expectedQuery: `SELECT a.id, SUM(t.streams_millions) AS streams
FROM tracks t
JOIN albums a ON t.album_id = a.id
GROUP BY a.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 5 },
      parTimeSeconds: 220,
      xpReward: 125,
      relevantTables: ['tracks', 'albums'],
    },
    {
      title: 'Fresh drops',
      isBoss: false,
      story: 'Albums released in 2023.',
      concept: 'WHERE year',
      task: 'title FROM albums WHERE release_year = 2023.',
      starterCode: 'SELECT title FROM albums WHERE release_year = 2023;',
      expectedQuery: 'SELECT title FROM albums WHERE release_year = 2023;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 1 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['albums'],
    },
    {
      title: 'Stream Boss: chart topper',
      isBoss: true,
      story: 'Track title with the highest streams_millions; tie-break smallest track id.',
      concept: 'ORDER BY',
      task: 'title FROM tracks ORDER BY streams_millions DESC, id ASC LIMIT 1.',
      starterCode:
        'SELECT title FROM tracks ORDER BY streams_millions DESC, id ASC LIMIT 1;',
      expectedQuery: 'SELECT title FROM tracks ORDER BY streams_millions DESC, id ASC LIMIT 1;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['title'],
      },
      parTimeSeconds: 150,
      xpReward: 270,
      relevantTables: ['tracks'],
    },
  ],
);

export const world10 = buildWorld(
  {
    id: 10,
    name: 'Lodging Ledger',
    subtitle: 'Short-term rentals',
    theme: 'lodging',
    description: 'Listings, reservations, reviews — host economics.',
    database: 'lodging_guests.sql',
    icon: '🏠',
    color: '#fb923c',
    prerequisites: [9],
  },
  [
    {
      title: 'Live listings',
      isBoss: false,
      story: 'Titles where active = 1.',
      concept: 'WHERE',
      task: 'title FROM listings WHERE active = 1.',
      starterCode: 'SELECT title FROM listings WHERE active = 1;',
      expectedQuery: 'SELECT title FROM listings WHERE active = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 75,
      xpReward: 60,
      relevantTables: ['listings'],
    },
    {
      title: 'Room for groups',
      isBoss: false,
      story: 'Properties that fit at least 4 guests.',
      concept: 'WHERE INTEGER',
      task: 'city FROM properties WHERE max_guests >= 4.',
      starterCode: 'SELECT city FROM properties WHERE max_guests >= 4;',
      expectedQuery: 'SELECT city FROM properties WHERE max_guests >= 4;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 90,
      xpReward: 70,
      relevantTables: ['properties'],
    },
    {
      title: 'Big stays',
      isBoss: false,
      story: 'Reservations over 400 total_price.',
      concept: 'WHERE REAL',
      task: 'guest_email, total_price FROM reservations WHERE total_price > 400.',
      starterCode: 'SELECT guest_email, total_price FROM reservations WHERE total_price > 400;',
      expectedQuery: 'SELECT guest_email, total_price FROM reservations WHERE total_price > 400;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['reservations'],
    },
    {
      title: 'City on the invoice',
      isBoss: false,
      story: 'Each reservation shows listing title and property city.',
      concept: 'Multi JOIN',
      task: 'JOIN reservations → listings → properties; return guest_email, listings.title, properties.city.',
      starterCode:
        'SELECT r.guest_email, l.title, p.city\nFROM reservations r\nJOIN listings l ON r.listing_id = l.id\nJOIN properties p ON ',
      expectedQuery: `SELECT r.guest_email, l.title, p.city
FROM reservations r
JOIN listings l ON r.listing_id = l.id
JOIN properties p ON l.property_id = p.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 5 },
      parTimeSeconds: 260,
      xpReward: 130,
      relevantTables: ['reservations', 'listings', 'properties'],
    },
    {
      title: 'Superhosts',
      isBoss: false,
      story: 'Host names flagged superhost.',
      concept: 'WHERE flag',
      task: 'name FROM hosts WHERE superhost = 1.',
      starterCode: 'SELECT name FROM hosts WHERE superhost = 1;',
      expectedQuery: 'SELECT name FROM hosts WHERE superhost = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['hosts'],
    },
    {
      title: 'Booking counts',
      isBoss: false,
      story: 'Reservations per listing_id.',
      concept: 'GROUP BY',
      task: 'listing_id, COUNT(*) AS n FROM reservations GROUP BY listing_id.',
      starterCode:
        'SELECT listing_id, COUNT(*) AS n FROM reservations GROUP BY listing_id;',
      expectedQuery: 'SELECT listing_id, COUNT(*) AS n FROM reservations GROUP BY listing_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 120,
      xpReward: 95,
      relevantTables: ['reservations'],
    },
    {
      title: 'Star averages',
      isBoss: false,
      story: 'Average review stars per listing_id.',
      concept: 'AVG GROUP BY',
      task: 'listing_id, AVG(stars) AS avg_stars FROM reviews GROUP BY listing_id.',
      starterCode:
        'SELECT listing_id, AVG(stars) AS avg_stars FROM reviews GROUP BY listing_id;',
      expectedQuery: 'SELECT listing_id, AVG(stars) AS avg_stars FROM reviews GROUP BY listing_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 130,
      xpReward: 100,
      relevantTables: ['reviews'],
    },
    {
      title: 'Loft reputation',
      isBoss: false,
      story: 'Average stars for listing_id 1 only.',
      concept: 'AVG + WHERE',
      task: 'AVG(stars) FROM reviews WHERE listing_id = 1.',
      starterCode: 'SELECT AVG(stars) AS avg_stars FROM reviews WHERE listing_id = 1;',
      expectedQuery: 'SELECT AVG(stars) AS avg_stars FROM reviews WHERE listing_id = 1;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['avg_stars'],
      },
      parTimeSeconds: 100,
      xpReward: 85,
      relevantTables: ['reviews'],
    },
    {
      title: 'Miami nights rate',
      isBoss: false,
      story: 'nightly_rate for Miami property.',
      concept: 'WHERE city',
      task: 'nightly_rate FROM properties WHERE city = Miami.',
      starterCode: "SELECT nightly_rate FROM properties WHERE city = 'Miami';",
      expectedQuery: "SELECT nightly_rate FROM properties WHERE city = 'Miami';",
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['nightly_rate'],
      },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['properties'],
    },
    {
      title: 'Lodging Boss: revenue listing',
      isBoss: true,
      story: 'listing_id with highest SUM(total_price); tie-break smallest listing_id.',
      concept: 'SUM + GROUP BY',
      task: 'SUM(total_price) GROUP BY listing_id ORDER BY s DESC, listing_id ASC LIMIT 1.',
      starterCode:
        'SELECT listing_id, SUM(total_price) AS s\nFROM reservations\nGROUP BY listing_id\nORDER BY s DESC, listing_id ASC\nLIMIT 1;',
      expectedQuery: `SELECT listing_id, SUM(total_price) AS s
FROM reservations
GROUP BY listing_id
ORDER BY s DESC, listing_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['listing_id', 's'],
      },
      parTimeSeconds: 220,
      xpReward: 290,
      relevantTables: ['reservations'],
    },
  ],
);

export const world11 = buildWorld(
  {
    id: 11,
    name: 'People Ops Peak',
    subtitle: 'HR reporting',
    theme: 'hr',
    description: 'Departments, employees, time off — workforce metrics.',
    database: 'hr_portal.sql',
    icon: '👥',
    color: '#60a5fa',
    prerequisites: [10],
  },
  [
    {
      title: 'Engineering crew',
      isBoss: false,
      story: 'Employees in department_id 1.',
      concept: 'WHERE',
      task: 'first_name, last_name FROM employees WHERE department_id = 1.',
      starterCode:
        'SELECT first_name, last_name FROM employees WHERE department_id = 1;',
      expectedQuery: 'SELECT first_name, last_name FROM employees WHERE department_id = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['employees'],
    },
    {
      title: 'Six-figure club',
      isBoss: false,
      story: 'Salaries strictly above 100000.',
      concept: 'WHERE REAL',
      task: 'first_name, salary FROM employees WHERE salary > 100000.',
      starterCode: 'SELECT first_name, salary FROM employees WHERE salary > 100000;',
      expectedQuery: 'SELECT first_name, salary FROM employees WHERE salary > 100000;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 5 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['employees'],
    },
    {
      title: 'Headcount by dept',
      isBoss: false,
      story: 'Employee count per department_id.',
      concept: 'GROUP BY',
      task: 'department_id, COUNT(*) AS n FROM employees GROUP BY department_id.',
      starterCode:
        'SELECT department_id, COUNT(*) AS n FROM employees GROUP BY department_id;',
      expectedQuery: 'SELECT department_id, COUNT(*) AS n FROM employees GROUP BY department_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 4 },
      parTimeSeconds: 110,
      xpReward: 90,
      relevantTables: ['employees'],
    },
    {
      title: 'Org chart names',
      isBoss: false,
      story: 'Employee first_name with department name.',
      concept: 'JOIN',
      task: 'employees JOIN departments; first_name, departments.name AS dept_name.',
      starterCode:
        'SELECT e.first_name, d.name AS dept_name\nFROM employees e\nJOIN departments d ON e.department_id = d.id;',
      expectedQuery: `SELECT e.first_name, d.name AS dept_name
FROM employees e
JOIN departments d ON e.department_id = d.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 8 },
      parTimeSeconds: 180,
      xpReward: 105,
      relevantTables: ['employees', 'departments'],
    },
    {
      title: 'Pending PTO',
      isBoss: false,
      story: 'Time off requests still pending.',
      concept: 'WHERE TEXT',
      task: 'employee_id FROM time_off WHERE status = pending.',
      starterCode: "SELECT employee_id FROM time_off WHERE status = 'pending';",
      expectedQuery: "SELECT employee_id FROM time_off WHERE status = 'pending';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['time_off'],
    },
    {
      title: 'Eng average pay',
      isBoss: false,
      story: 'Average salary in department_id 1.',
      concept: 'AVG + WHERE',
      task: 'AVG(salary) FROM employees WHERE department_id = 1.',
      starterCode: 'SELECT AVG(salary) AS avg_sal FROM employees WHERE department_id = 1;',
      expectedQuery: 'SELECT AVG(salary) AS avg_sal FROM employees WHERE department_id = 1;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['avg_sal'],
      },
      parTimeSeconds: 100,
      xpReward: 85,
      relevantTables: ['employees'],
    },
    {
      title: 'Veterans',
      isBoss: false,
      story: 'Hired before 2020-01-01.',
      concept: 'WHERE DATE',
      task: 'last_name, hire_date WHERE hire_date < 2020-01-01.',
      starterCode:
        "SELECT last_name, hire_date FROM employees WHERE hire_date < '2020-01-01';",
      expectedQuery:
        "SELECT last_name, hire_date FROM employees WHERE hire_date < '2020-01-01';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 100,
      xpReward: 80,
      relevantTables: ['employees'],
    },
    {
      title: 'Top earner',
      isBoss: false,
      story: 'The single highest salary in the company.',
      concept: 'MAX',
      task: 'MAX(salary) AS top FROM employees.',
      starterCode: 'SELECT MAX(salary) AS top FROM employees;',
      expectedQuery: 'SELECT MAX(salary) AS top FROM employees;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['top'],
      },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['employees'],
    },
    {
      title: 'Budget roll-up',
      isBoss: false,
      story: 'Total department budget.',
      concept: 'SUM',
      task: 'SUM(budget) AS total_budget FROM departments.',
      starterCode: 'SELECT SUM(budget) AS total_budget FROM departments;',
      expectedQuery: 'SELECT SUM(budget) AS total_budget FROM departments;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['total_budget'],
      },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['departments'],
    },
    {
      title: 'HR Boss: richest department',
      isBoss: true,
      story: 'department_id with highest average employee salary; tie-break smallest id.',
      concept: 'AVG + GROUP BY',
      task: 'AVG(salary) GROUP BY department_id ORDER BY a DESC, department_id ASC LIMIT 1.',
      starterCode:
        'SELECT department_id, AVG(salary) AS a\nFROM employees\nGROUP BY department_id\nORDER BY a DESC, department_id ASC\nLIMIT 1;',
      expectedQuery: `SELECT department_id, AVG(salary) AS a
FROM employees
GROUP BY department_id
ORDER BY a DESC, department_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['department_id', 'a'],
      },
      parTimeSeconds: 240,
      xpReward: 300,
      relevantTables: ['employees'],
    },
  ],
);

export const world12 = buildWorld(
  {
    id: 12,
    name: 'Cinema Circuit',
    subtitle: 'Theaters & tickets',
    theme: 'cinema',
    description: 'Theaters, movies, showtimes, tickets — show scheduling.',
    database: 'cinema_box.sql',
    icon: '🎬',
    color: '#e879f9',
    prerequisites: [11],
  },
  [
    {
      title: 'Multiplex map',
      isBoss: false,
      story: 'Theaters with at least 8 screens.',
      concept: 'WHERE',
      task: 'name FROM theaters WHERE screens >= 8.',
      starterCode: 'SELECT name FROM theaters WHERE screens >= 8;',
      expectedQuery: 'SELECT name FROM theaters WHERE screens >= 8;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 60,
      relevantTables: ['theaters'],
    },
    {
      title: 'Teen-friendly slate',
      isBoss: false,
      story: 'PG-13 movie titles.',
      concept: 'WHERE TEXT',
      task: 'title FROM movies WHERE rating = PG-13.',
      starterCode: "SELECT title FROM movies WHERE rating = 'PG-13';",
      expectedQuery: "SELECT title FROM movies WHERE rating = 'PG-13';",
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['movies'],
    },
    {
      title: 'Budget matinee',
      isBoss: false,
      story: 'Showtimes priced under 12.',
      concept: 'WHERE REAL',
      task: 'id, movie_id, price WHERE price < 12.',
      starterCode: 'SELECT id, movie_id, price FROM showtimes WHERE price < 12;',
      expectedQuery: 'SELECT id, movie_id, price FROM showtimes WHERE price < 12;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 90,
      xpReward: 75,
      relevantTables: ['showtimes'],
    },
    {
      title: 'Marquee lines',
      isBoss: false,
      story: 'Movie title and show_time for every showtime.',
      concept: 'JOIN',
      task: 'showtimes JOIN movies ON movie_id; movies.title, showtimes.show_time.',
      starterCode:
        'SELECT m.title, s.show_time\nFROM showtimes s\nJOIN movies m ON s.movie_id = m.id;',
      expectedQuery: `SELECT m.title, s.show_time
FROM showtimes s
JOIN movies m ON s.movie_id = m.id;`,
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 6 },
      parTimeSeconds: 180,
      xpReward: 110,
      relevantTables: ['showtimes', 'movies'],
    },
    {
      title: 'Screens booked',
      isBoss: false,
      story: 'Showtime count per theater.',
      concept: 'GROUP BY',
      task: 'theater_id, COUNT(*) AS n FROM showtimes GROUP BY theater_id.',
      starterCode:
        'SELECT theater_id, COUNT(*) AS n FROM showtimes GROUP BY theater_id;',
      expectedQuery: 'SELECT theater_id, COUNT(*) AS n FROM showtimes GROUP BY theater_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 3 },
      parTimeSeconds: 110,
      xpReward: 90,
      relevantTables: ['showtimes'],
    },
    {
      title: 'Ticket sales',
      isBoss: false,
      story: 'Tickets per showtime_id.',
      concept: 'GROUP BY',
      task: 'showtime_id, COUNT(*) AS n FROM tickets GROUP BY showtime_id.',
      starterCode: 'SELECT showtime_id, COUNT(*) AS n FROM tickets GROUP BY showtime_id;',
      expectedQuery: 'SELECT showtime_id, COUNT(*) AS n FROM tickets GROUP BY showtime_id;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 5 },
      parTimeSeconds: 110,
      xpReward: 90,
      relevantTables: ['tickets'],
    },
    {
      title: 'Epic runtime',
      isBoss: false,
      story: 'Longest movie by runtime_min.',
      concept: 'ORDER BY',
      task: 'title ORDER BY runtime_min DESC LIMIT 1.',
      starterCode: 'SELECT title FROM movies ORDER BY runtime_min DESC LIMIT 1;',
      expectedQuery: 'SELECT title FROM movies ORDER BY runtime_min DESC LIMIT 1;',
      validation: { strategy: ['result_match'], orderSensitive: true, expectedRowCount: 1 },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['movies'],
    },
    {
      title: 'Floor price',
      isBoss: false,
      story: 'Cheapest showtime ticket price in the table.',
      concept: 'MIN',
      task: 'MIN(price) AS lo FROM showtimes.',
      starterCode: 'SELECT MIN(price) AS lo FROM showtimes;',
      expectedQuery: 'SELECT MIN(price) AS lo FROM showtimes;',
      validation: {
        strategy: ['result_match'],
        orderSensitive: false,
        expectedColumns: ['lo'],
      },
      parTimeSeconds: 75,
      xpReward: 65,
      relevantTables: ['showtimes'],
    },
    {
      title: 'Star Query showings',
      isBoss: false,
      story: 'Showtimes for movie_id 1.',
      concept: 'WHERE FK',
      task: 'show_time FROM showtimes WHERE movie_id = 1.',
      starterCode: 'SELECT show_time FROM showtimes WHERE movie_id = 1;',
      expectedQuery: 'SELECT show_time FROM showtimes WHERE movie_id = 1;',
      validation: { strategy: ['result_match'], orderSensitive: false, expectedRowCount: 2 },
      parTimeSeconds: 75,
      xpReward: 70,
      relevantTables: ['showtimes'],
    },
    {
      title: 'Cinema Boss: marquee frequency',
      isBoss: true,
      story: 'movie_id appearing in the most showtimes; tie-break smallest movie_id.',
      concept: 'COUNT + GROUP BY',
      task: 'COUNT(*) GROUP BY movie_id ORDER BY c DESC, movie_id ASC LIMIT 1.',
      starterCode:
        'SELECT movie_id, COUNT(*) AS c\nFROM showtimes\nGROUP BY movie_id\nORDER BY c DESC, movie_id ASC\nLIMIT 1;',
      expectedQuery: `SELECT movie_id, COUNT(*) AS c
FROM showtimes
GROUP BY movie_id
ORDER BY c DESC, movie_id ASC
LIMIT 1;`,
      validation: {
        strategy: ['result_match'],
        orderSensitive: true,
        expectedRowCount: 1,
        expectedColumns: ['movie_id', 'c'],
      },
      parTimeSeconds: 200,
      xpReward: 285,
      relevantTables: ['showtimes'],
    },
  ],
);
